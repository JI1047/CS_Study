## EFS (Elastic File System)

---

### 🧩 개념

> AWS에서 제공하는 확장 가능하고 완전 관리형 네트워크 파일 시스템 (NFS 기반)
> 
> 
> 여러 **EC2 인스턴스가 동시에 같은 파일 시스템을 읽고 쓸 수 있도록** 공유 스토리지를 제공한다.
> 
- 여러 서버가 동시에 접근 가능한 “공용 HDD” 개념
- EC2에 직접 연결(EBS)하는 방식이 아닌, **네트워크를 통해 접근**
- 표준 **NFS(Network File System)** 프로토콜 지원 → 리눅스 서버에서 마운트 가능
- AWS가 자동으로 용량을 확장하고, 고가용성(HA)을 보장함

---

### ⚙️ 동작 구조

1. EFS 파일 시스템 생성
2. EC2 인스턴스에 **마운트(Mount)** 설정
3. 각 EC2에서 `/mnt/efs` 같은 디렉토리를 통해 접근
4. 읽기/쓰기 가능 — 동시에 접근 시 **파일 락(lock)** 으로 동시성 제어
5. 네트워크 기반이라 EC2를 껐다 켜도 데이터 유지

> 💬 즉, 여러 인스턴스가 한 폴더를 같이 쓰는 구조
> 

---

### 📦 EFS vs EBS vs S3 비교

| 항목 | EFS | EBS | S3 |
| --- | --- | --- | --- |
| 형태 | 네트워크 파일 시스템 | 블록 스토리지 | 객체 스토리지 |
| 연결 방식 | 여러 EC2 동시 접근 가능 | 단일 EC2 전용 | API로 접근 |
| 사용 예시 | 웹 서버 공용 파일, 세션, AI 모델 공유 | OS, DB 등 인스턴스 전용 디스크 | 이미지, 백업, 로그 저장 |
| 프로토콜 | NFS | 디스크 블록 수준 | HTTP 기반 REST API |
| 확장성 | 자동 확장 | 수동 확장 | 자동 확장 |
| 사용 예 | 다중 서버 웹 앱, 공유 리소스 | DB, 로그 저장소 | 정적 파일, CDN, 백업 |

---

### 🧠 사용 이유

> 여러 EC2 인스턴스(즉, 여러 서버)가 공유 파일 시스템을 동시에 사용해야 할 때
> 
- 모든 인스턴스에서 동일한 파일에 접근해야 하는 경우
- 공용 애플리케이션 파일, 이미지, 세션 데이터를 여러 서버에서 공유해야 할 때
- AI/ML 모델, 미디어 처리, 대용량 데이터 분석 등 **파일 기반 데이터 공유** 필요 시
- CI/CD 환경에서 빌드 산출물이나 로그를 여러 서버에서 동시에 접근할 때

---

### 💡 예시

### ✅ 1. 다중 웹 서버 환경

- 100개의 EC2 인스턴스가 동일한 웹 애플리케이션을 서빙해야 할 때
- 코드(JAR, Node.js 등), 정적 파일, 세션 데이터를 **EFS에 저장**
- 모든 EC2에서 EFS를 마운트 → 공용 스토리지로 활용

### ✅ 2. AI 모델 서빙

- AI 모델 파일이 수 GB 단위로 매우 큼
- EC2 프리티어 기준 디스크 용량(30GB)이 한정적
- 모델 파일을 EFS에 올려두고 여러 EC2가 동시에 참조

### ✅ 3. CI/CD 환경

- 빌드 서버, 테스트 서버, 배포 서버 간에 공용 빌드 결과물을 공유
- Jenkins나 CodeBuild 같은 도구에서 EFS를 공유 스토리지로 사용 가능

---

### 🔗 주요 개념

### 🧷 마운트(Mount)

- EFS를 EC2에서 로컬 디렉토리처럼 연결하는 과정
- 마운트 후에는 `/mnt/efs` 경로 아래에서 파일을 일반 디스크처럼 사용 가능

---

### 🛠️ 아키텍처 예시

```
[ EFS ]
   │
 ┌─┴───────────────┐
 │                 │
[EC2 #1]       [EC2 #2]
(웹서버)       (WAS서버)
  │                 │
  └────── 공유 파일 접근 ──────┘

```

---

### ✅ 요약

> EFS는 여러 EC2 인스턴스가 동시에 접근 가능한 네트워크 파일 스토리지
> 
> 
> 공용 파일, AI 모델, 빌드 결과 등 공유 리소스를 저장할 때 사용하며
> 
> EBS보다 확장성이 높고 S3보다 파일 접근성이 뛰어나다.
>
