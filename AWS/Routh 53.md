### 🧩 개념

> AWS에서 제공하는 확장 가능한 클라우드 기반 도메인 네임 시스템(DNS) 서비스
> 
> 
> 사용자가 입력한 도메인 이름(`www.example.com`)을 실제 서버의 IP 주소(`192.0.2.1`)로 변환해주는 역할을 수행한다.
> 
- 인터넷의 “전화번호부” 역할
- 트래픽을 올바른 서버(예: EC2, Load Balancer, S3 등)로 **분배(라우팅)**
- AWS 내부 리소스뿐만 아니라 외부 인프라와도 연동 가능

---

### ⚙️ 주요 기능

| 기능 | 설명 |
| --- | --- |
| **도메인 등록 (Domain Registration)** | AWS에서 직접 도메인 구입 및 관리 가능 |
| **DNS 레코드 관리 (DNS Management)** | A, AAAA, CNAME, MX, TXT 등 다양한 레코드 생성 및 설정 |
| **트래픽 라우팅 (Traffic Routing)** | 요청을 여러 리소스로 분산시켜 가용성과 성능 향상 |
| **헬스 체크 (Health Check)** | 지정된 엔드포인트의 상태를 모니터링하고, 비정상 리소스로 트래픽 전송 차단 |
| **통합 관리** | CloudFront, ALB, EC2, S3 등 AWS 서비스와 긴밀하게 연동 |

---

### 🚦 라우팅 정책 종류

| 라우팅 정책 | 설명 |
| --- | --- |
| **단순(Simple)** | 단일 리소스로 트래픽 라우팅 |
| **가중치 기반(Weighted)** | 여러 대상에 트래픽 비율을 지정해 분산 |
| **지리적 위치 기반(Geolocation)** | 사용자의 위치(국가/대륙)에 따라 가장 가까운 리소스로 연결 |
| **지연 시간 기반(Latency)** | 네트워크 지연이 가장 낮은 리전으로 트래픽 라우팅 |
| **장애 조치(Failover)** | 기본 리소스가 장애 시 예비 리소스로 자동 전환 |
| **멀티밸류 응답(Multi-Value Answer)** | 여러 IP를 반환하여 가용성 향상 (단순 로드밸런싱 효과) |

---

### 🧭 사용 이유

- 안정적이고 빠른 DNS 응답 속도
- **트래픽 분산 + 장애 대응** 기능으로 서비스 가용성 향상
- AWS 인프라와 완벽히 통합되어 관리 효율 극대화
- 글로벌 인프라를 통한 **지연 시간 최소화**

> 💡 예시
> 
> - 사용자가 서울에서 접속하면 “서울 리전” EC2로
> - 미국에서 접속하면 “버지니아 리전” EC2로 자동 연결

---

---

### ✅ 요약

> Route 53은 AWS의 DNS 및 트래픽 제어 핵심 서비스
> 
> - 도메인 등록 + 트래픽 라우팅 + 헬스 체크 통합 관리
> - 글로벌 DNS 인프라 기반으로 빠르고 안정적인 사용자 연결 보장
