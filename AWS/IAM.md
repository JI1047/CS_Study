## 🔐 IAM (Identity and Access Management)

### 🌩️ 클라우드 네이티브 개념과 IAM의 역할

> 클라우드 환경을 “최적화 + 안정 + 보안” 관점에서 운영하기 위한 핵심 서비스
> 

### 💡 클라우드 네이티브란?

> 클라우드 자원을 효율적으로 이용하고, 안정적·보안적으로 운영하기 위한 방식
> 

| 구분 | 설명 |
| --- | --- |
| **이용 최적화** | 클라우드는 사용량 기반 과금 → 자원을 최소한으로 효율적으로 사용해야 함 |
| **안정성 확보** | 내가 운영하는 서비스이므로, 장애나 오남용 없이 안정적으로 유지해야 함 |
| **보안 강화** | 조직 내 접근 권한을 세밀히 제어하여 실수나 외부 침입을 방지 |

> ⚠️ 예시:
> 
> - 신입에게 전체 리소스 접근을 허용하면 실수로 서버를 삭제할 수도 있음
> - IAM을 통해 **“누가 무엇을 할 수 있는지”**를 명확히 통제해야 함
> - 2차 인증(MFA/OTP) 반드시 설정해야 함

---

### 🧭 IAM 개념

> AWS 리소스에 대한 인증(Authentication) 과
> 
> 
> **권한 관리(Authorization)** 를 담당하는 서비스
> 
- AWS 계정 내에서 사용자별 접근 제어를 수행
- 팀이나 역할에 맞게 권한을 세분화하여 관리 가능
    
    (예: 신입 → ReadOnly, FE팀 → S3 접근 가능, BE팀 → EC2/WAS 접근 가능)
    

---

### ⚙️ 구성 요소

| 구성요소 | 설명 |
| --- | --- |
| **사용자(User)** | AWS 리소스에 접근 가능한 개별 계정 (ID/비밀번호 or Access Key) |
| **그룹(Group)** | 여러 사용자를 묶어 공통된 권한 부여 (예: FE팀, BE팀, DevOps팀) |
| **역할(Role)** | 임시로 다른 사용자 또는 AWS 서비스에 특정 권한 위임 (예: EC2가 S3 접근할 때) |
| **정책(Policy)** | JSON 형태로 “누가 어떤 리소스에 어떤 행동을 할 수 있는가”를 명시하는 권한 규칙 |

> 💬 “정책”이 IAM의 핵심이다.
> 
> 
> 예를 들어 AmazonS3ReadOnlyAccess 정책을 FE팀에 부여하면
> 
> S3 버킷에서 조회만 가능하고 수정은 불가능하다.
> 

---

### 🎯 사용 이유

- 조직 내 사용자와 리소스에 대한 **세밀한 권한 제어**
- 최소 권한 원칙(Principle of Least Privilege)을 통한 **보안성 강화**
- 서비스 운영 중 **인적 실수로 인한 장애 최소화**
- 인증, 권한, MFA 등 **보안 관리의 중앙화**

---

### ✅ 핵심 요약

> IAM은 “사람과 리소스의 보안 관문”
> 
> - 최소 권한 원칙
> - 역할 기반 접근 제어
> - MFA 필수 설정
>     
>     이 세 가지가 보안의 기본 구조다.
>
