## 운영체제의 모드 구조 (User Mode vs Kernel Mode)

---

### 🧩 개념

운영체제는 안정성과 보안을 위해 **프로그램 실행 권한 수준**을 두 가지 모드로 구분한다.

| 구분 | User Mode | Kernel Mode |
| --- | --- | --- |
| **실행 주체** | 일반 사용자 프로그램 (예: 브라우저, 메모장 등) | 운영체제 핵심(커널, 드라이버 등) |
| **권한 수준** | 제한적 (시스템 자원 직접 접근 불가) | 최고 권한 (모든 하드웨어/자원 접근 가능) |
| **접근 가능 자원** | 사용자 영역 메모리만 접근 가능 | 전체 메모리, CPU, 디스크, I/O 장치 접근 가능 |
| **안정성** | 오류 발생 시 해당 프로세스만 영향 | 오류 시 시스템 전체 영향을 줄 수 있음 |
| **전환 방식** | 시스템 호출(System Call)을 통해 커널에 요청 | 요청 처리 후 User Mode로 복귀 |

> 💡 운영체제는 User Mode와 Kernel Mode를 엄격히 구분하여
> 
> 
> 사용자 프로그램이 직접 하드웨어에 접근하는 것을 방지한다.
> 
> → 시스템 안정성, 보안성 보장
> 

---

### 🧠 동작 방식

### **1️⃣ 사용자 요청 생성 (User Mode)**

- 사용자는 응용 프로그램(예: 메모장)을 통해 작업 요청을 수행
- 응용 프로그램은 직접 하드웨어 접근 불가
- → **시스템 호출(System Call)** 을 통해 커널에 요청 전달

```
User → [System Call 요청] → Kernel

```

---

### **2️⃣ 시스템 호출에 따른 Kernel Mode 전환**

- 시스템 호출이 발생하면 CPU는 **User Mode → Kernel Mode** 로 전환
- 커널이 CPU 제어권을 가져와 요청된 작업을 처리

---

### **3️⃣ 커널에서 요청 처리 (Kernel Mode)**

- 커널은 시스템 자원과 하드웨어를 직접 제어

---

### **4️⃣ 결과 반환 (Kernel → User Mode 복귀)**

- 요청이 완료되면 커널은 결과(성공/실패 여부)를 응용 프로그램에 전달
- CPU는 다시 **Kernel Mode → User Mode** 로 전환

---

### **5️⃣ 사용자 프로그램 처리 (User Mode)**

- 응용 프로그램이 결과를 받아 사용자에게 표시
- 이후 추가 작업 수행 가능

---

### 📄 전체 흐름 예시

> “메모장에 startupcode.txt 파일을 저장하는 과정”
> 
1. 사용자가 **메모장에서 “저장” 버튼 클릭**
2. 메모장은 User Mode에서 파일 저장 요청 생성
3. 시스템 호출을 통해 Kernel Mode로 전환
4. 커널이 파일 시스템과 디스크 드라이버를 이용해 파일 저장
5. 저장 완료 후 결과를 메모장에 반환, User Mode로 복귀
6. 메모장은 “startupcode.txt 저장 완료” 메시지 표시

---

### 🔒 전환의 필요성

| 목적 | 설명 |
| --- | --- |
| **안정성** | 사용자 프로그램이 커널 영역에 직접 접근하지 못하도록 격리 |
| **보안성** | 악의적 프로그램이 하드웨어 제어권 탈취 방지 |
| **효율성** | 시스템 자원 요청 시 커널이 직접 처리하여 일관성 유지 |

---

### 🧩 요약

> User Mode: 사용자 프로그램이 동작하는 제한된 환경
> 
> 
> **Kernel Mode**: 운영체제가 하드웨어를 제어하는 핵심 영역
> 
> 두 모드는 **시스템 호출(System Call)** 을 통해 전환되며,
> 
> 이 구조 덕분에 운영체제는 **보안, 안정성, 효율성**을 모두 유지한다.
>
